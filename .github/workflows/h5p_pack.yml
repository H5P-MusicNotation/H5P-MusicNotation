name: Pack H5P Content Type H5P-Music-Notation

on: 
  push:
    branches: [main]

jobs:
  pack:
    name: Pack with H5P-Music-Notation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Install npm 
        run: npm ci
      - name: List files before packing
        run: |
          pwd
          ls
      - name: run h5p pack
        run: |
          npm install -g h5p
          cd ..
          pwd
          h5p pack H5P-MusicNotation H5P-MusicNotation/h5p-package/h5p-musicnotation.h5p
      - name: List files after packing
        run: |
          pwd
          ls

